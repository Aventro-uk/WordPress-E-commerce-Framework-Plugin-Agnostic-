# Setup

## Requirements

- WordPress 5.0+
- PHP 7.4+
- Composer
- Node.js & npm

## Installation

1. **Clone the repository** into your WordPress installation’s `wp-content/` folder (or set the web root to the repository root if you keep WordPress elsewhere).

2. **Install PHP dependencies**:
   ```bash
   composer install
   cd wp-content/plugins/ecom-core && composer install
